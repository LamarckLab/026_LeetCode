## Lamarck &nbsp; &nbsp; &nbsp;
#### 0002  两数相加
---


*01  迭代法*
```python
# Definition for singly-linked list.
# class ListNode(object):
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution(object):
    def addTwoNumbers(self, l1, l2):
        """
        :type l1: Optional[ListNode]
        :type l2: Optional[ListNode]
        :rtype: Optional[ListNode]
        """
        result = ListNode()
        cur = result
        next1 = 0
        while l1 != None and l2 != None:
            digit_sum = l1.val + l2.val + next1
            cur.next = ListNode(digit_sum%10)
            next1 = digit_sum // 10
            l1, l2, cur = l1.next, l2.next, cur.next
        while l1 != None:
            digit_sum = l1.val + next1
            cur.next = ListNode(digit_sum%10)
            next1 = digit_sum // 10
            l1, cur = l1.next, cur.next
        while l2 != None:
            digit_sum = l2.val +next1
            cur.next = ListNode(digit_sum%10)
            next1 = digit_sum // 10
            l2, cur = l2.next, cur.next
        if next1 == 1:
            cur.next = ListNode(1)
        return result.next
```